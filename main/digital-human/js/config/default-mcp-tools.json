[
    {
        "name": "self_camera_take_photo",
        "description": "Take a photo using the device's camera. This tool captures the current camera frame and returns the image data. You MUST call this tool when user asks to 'take a photo', 'what do you see', 'describe what you see', or similar requests. After getting the photo data, describe what you see in the image. Parameter 'question' is optional, defaults to 'describe objects seen'.",
        "inputSchema": {
            "type": "object",
            "properties": {
                "question": {
                    "type": "string",
                    "description": "Question to guide the photo analysis, e.g., 'describe the objects seen'. Can be empty."
                }
            }
        },
        "mockResponse": {
            "success": true,
            "photo_data": "base64_image_data",
            "response": "Please describe what you see based on the returned photo_data"
        }
    },
    {
        "name": "self.get_device_status",
        "description": "Provides the real-time information of the device, including the current status of the audio speaker, screen, battery, network, etc.\nUse this tool for: \n1. Answering questions about current condition (e.g. what is the current volume of the audio speaker?)\n2. As the first step to control the device (e.g. turn up / down the volume of the audio speaker, etc.)",
        "inputSchema": {
            "type": "object",
            "properties": {}
        },
        "mockResponse": {
            "audio_speaker": {
                "volume": 50,
                "muted": false
            },
            "screen": {
                "brightness": 80,
                "theme": "light"
            },
            "battery": {
                "level": 85,
                "charging": false
            },
            "network": {
                "connected": true,
                "type": "wifi"
            }
        }
    },
    {
        "name": "self.audio_speaker.set_volume",
        "description": "Set the volume of the audio speaker. If the current volume is unknown, you must call `self.get_device_status` tool first and then call this tool.",
        "inputSchema": {
            "type": "object",
            "properties": {
                "volume": {
                    "type": "integer",
                    "minimum": 0,
                    "maximum": 100
                }
            },
            "required": [
                "volume"
            ]
        },
        "mockResponse": {
            "success": true,
            "volume": "${volume}",
            "message": "音量已设置为 ${volume}"
        }
    },
    {
        "name": "self.screen.set_brightness",
        "description": "Set the brightness of the screen.",
        "inputSchema": {
            "type": "object",
            "properties": {
                "brightness": {
                    "type": "integer",
                    "minimum": 0,
                    "maximum": 100
                }
            },
            "required": [
                "brightness"
            ]
        },
        "mockResponse": {
            "success": true,
            "brightness": "${brightness}",
            "message": "亮度已设置为 ${brightness}"
        }
    },
    {
        "name": "self.motor.stop",
        "description": "Stop all motors. Use for: dừng, dừng lại, stop.",
        "inputSchema": { "type": "object", "properties": {} },
        "mockResponse": { "success": true, "action": "stop", "duration_ms": 0 }
    },
    {
        "name": "self.motor.move",
        "description": "Drive with per-wheel speed and duration_ms auto-stop (preferred for mv:* tags).",
        "inputSchema": {
            "type": "object",
            "properties": {
                "left": { "type": "integer", "minimum": -100, "maximum": 100 },
                "right": { "type": "integer", "minimum": -100, "maximum": 100 },
                "duration_ms": { "type": "integer", "minimum": 100, "maximum": 30000 }
            },
            "required": ["left", "right", "duration_ms"]
        },
        "mockResponse": {
            "success": true,
            "action": "move",
            "duration_ms": "${duration_ms}",
            "simulated": true
        }
    },
    {
        "name": "self.motor.forward",
        "description": "Move robot forward. Use for: đi tới, tiến, đi thẳng, go forward.",
        "inputSchema": {
            "type": "object",
            "properties": {
                "duration_ms": { "type": "integer", "minimum": 100, "maximum": 30000, "description": "Auto-stop after N ms (from server mv:f:N tag)" }
            }
        },
        "mockResponse": { "success": true, "action": "forward", "duration_ms": "${duration_ms}" }
    },
    {
        "name": "self.motor.backward",
        "description": "Move robot backward. Use for: lùi, đi lùi, go back.",
        "inputSchema": {
            "type": "object",
            "properties": {
                "duration_ms": { "type": "integer", "minimum": 100, "maximum": 30000 }
            }
        },
        "mockResponse": { "success": true, "action": "backward", "duration_ms": "${duration_ms}" }
    },
    {
        "name": "self.motor.turn_left",
        "description": "Turn robot left in place. Use for: quay sang trái, rẽ trái, turn left.",
        "inputSchema": {
            "type": "object",
            "properties": {
                "duration_ms": { "type": "integer", "minimum": 100, "maximum": 30000 }
            }
        },
        "mockResponse": { "success": true, "action": "turn_left", "duration_ms": "${duration_ms}" }
    },
    {
        "name": "self.motor.turn_right",
        "description": "Turn robot right in place. Use for: quay sang phải, rẽ phải, turn right.",
        "inputSchema": {
            "type": "object",
            "properties": {
                "duration_ms": { "type": "integer", "minimum": 100, "maximum": 30000 }
            }
        },
        "mockResponse": { "success": true, "action": "turn_right", "duration_ms": "${duration_ms}" }
    },
    {
        "name": "self.motor.dance",
        "description": "Dance with embedded music. track=1 slap-house ~23s; track=2 hip-hop ~99s; track=3 drill ~25s.",
        "inputSchema": {
            "type": "object",
            "properties": {
                "track": { "type": "integer", "minimum": 1, "maximum": 3, "default": 1 }
            }
        },
        "mockResponse": { "success": true, "action": "dance", "track": "${track}" }
    },
    {
        "name": "self.chassis.dance",
        "description": "Dance routine (chassis alias).",
        "inputSchema": { "type": "object", "properties": {} },
        "mockResponse": { "success": true, "action": "dance" }
    },
    {
        "name": "self.chassis.turn_left",
        "description": "Turn left (chassis alias).",
        "inputSchema": { "type": "object", "properties": {} },
        "mockResponse": { "success": true, "action": "turn_left" }
    },
    {
        "name": "self.chassis.turn_right",
        "description": "Turn right (chassis alias).",
        "inputSchema": { "type": "object", "properties": {} },
        "mockResponse": { "success": true, "action": "turn_right" }
    },
    {
        "name": "self.chassis.go_forward",
        "description": "Move forward (chassis alias).",
        "inputSchema": { "type": "object", "properties": {} },
        "mockResponse": { "success": true, "action": "forward" }
    },
    {
        "name": "self.chassis.go_back",
        "description": "Move backward (chassis alias).",
        "inputSchema": { "type": "object", "properties": {} },
        "mockResponse": { "success": true, "action": "backward" }
    }
]